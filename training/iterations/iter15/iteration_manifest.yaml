iteration: 15
latency_backend_name: "evolved"  # Backend name stays constant; implementation evolves
modified_files:
  - "sim/latency/evolved_model.go"
reasoning: |
  Iteration 15: Three-Axis Correction — Decode Amplification + MoE Non-Compute + Dense Batching

  **Context**: Iterations 10-14 failed catastrophically (loss 2000-4000%), with iter14's β₅ fix proving necessary but insufficient (loss improved only 2.8%: 2387% → 2319%). Root cause analysis of baseline simulator errors reveals the roofline model has THREE INDEPENDENT systematic errors:

  1. **Decode underestimation**: -90% to -95% ITL MPE across ALL models (roofline predicts decode 10-20× faster than reality)
  2. **MoE underestimation**: -69% avg TTFT MPE for Scout experiments (missing non-compute overhead: routing latency, load imbalance)
  3. **Dense overestimation**: +820% avg TTFT MPE for dense models (missing batching inefficiency in mixed prefill/decode batches)

  **Previous iteration (iter14)**: Fixed β₅ MoE gating layer multiplier bug (coefficient converged from 1924.4 to 32.5 ✓), but loss barely improved because OTHER coefficients (β₀, β₁, β₃, β₄, β₆) went haywire trying to compensate. Key learning: "Coefficient Convergence ≠ Performance Recovery" (iter14 Principle 1).

  **This iteration adds**:
  - **β₈ (MoE non-compute latency)**: Per-token routing overhead beyond FLOPs (expected 20-80 μs/token)
  - **β₉ (prefill batching penalty)**: Heterogeneity-induced MFU degradation (expected 0.5-2.0 μs/token)
  - **β₁, β₄ amplification**: Massive bound expansion (β₁: 1.0-1.15 → 5.0-15.0, β₄: 0.7-0.85 → 3.0-8.0) to match 10-20× decode slowdown
  - **Cold-start initialization**: Uniform random (NOT warm-start from iter7/iter14), following iter14 Principle 2 (dataset shifted)
  - **Increased optimization budget**: 1000 → 2000 trials (10-dimensional search requires more exploration)

  **Expected outcome**: Overall loss 2319% → <300% (≥87% improvement), with all three error patterns corrected simultaneously.
timestamp: "2026-03-31T18:00:00Z"

# Standard optimization command (default n_trials=2000 as of 2026-03-31)
optimization_command: "python3 inner_loop_optimize.py --iteration 15"
